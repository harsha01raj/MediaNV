import { LoggerInterceptorInterceptor } from './logger-interceptor.interceptor';

describe('LoggerInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new LoggerInterceptorInterceptor()).toBeDefined();
  });
});
