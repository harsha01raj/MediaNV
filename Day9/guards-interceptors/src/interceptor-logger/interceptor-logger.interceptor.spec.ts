import { InterceptorLoggerInterceptor } from './interceptor-logger.interceptor';

describe('InterceptorLoggerInterceptor', () => {
  it('should be defined', () => {
    expect(new InterceptorLoggerInterceptor()).toBeDefined();
  });
});
